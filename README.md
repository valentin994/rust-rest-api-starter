# Rust API Starter Project

API written in Axum that for now support CRUD operations on Users, 
the API connects to a postgres database.

## Supported Routes

TBD

## Authentication

TBD

## Dependencies

- Docker
- docker-compose
- rust
- cargo

## Installation

Run `$ docker-compose up` to setup the postgres database and adminer
for database management and exploration.

Run `$cargo run` to spin up the API

## Testing

TBD
